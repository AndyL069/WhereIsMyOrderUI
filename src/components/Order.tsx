import React, { Component } from 'react';

class Order extends Component {
    render() {
        return (
            <div>
                Create a new order
            </div>
        );
    }
}

export default Order;